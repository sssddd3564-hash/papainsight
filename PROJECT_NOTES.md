# 프로젝트 노트

이 문서는 파파인사이트의 확정된 작업 맥락과 운영 결정을 기록합니다.

## 현재 프로젝트

- 프로젝트명: 파파인사이트
- 성격: 파파컴퍼니 ERP(회사 내부 업무 관리 시스템) MVP(엠브이피: 최소 기능 제품)
- 기본 구조: 정적 `HTML`(에이치티엠엘: 화면 구조 코드), `CSS`(씨에스에스: 화면 스타일 코드), `JavaScript`(자바스크립트: 화면 동작 코드)
- 로컬 실행 주소: `http://localhost:4175/`
- 운영 도메인: `https://papainsight.co.kr/`

## 현재 주요 기능

- 직원 로그인 화면
- 영업자료 (이미지)
  - 카테고리별 이미지 등록
  - 이미지 다운로드/복사
  - 이미지 삭제
  - 삭제 기록 저장
  - 등록자 표시
  - 카테고리 접기/펼치기
  - 이미지 카드 펼쳐보기
- 영업자료 (텍스트)
  - 기존 파파AI 메뉴를 영업자료 문서 보관함으로 변경
  - `PAPAchatbot` 저장소에서 가져온 핵심 문서 2개를 표 중심으로 표시
- 거래처 관리
  - 담당자: 이신, 정완, 현민, 오찬, 도영
  - 담당자별 하위 메뉴 접기/펼치기
  - B2C / B2B 분리
  - 거래중 / 상담중 / 대기 상태 분리
- 정산관리
  - 현재 준비중 화면으로 표시

## 작업 운영 결정

- `GitHub`(깃허브: 코드 저장소 서비스) 저장, `commit`(커밋: 변경 내용을 저장소 기록으로 확정), `push`(푸시: 원격 저장소에 업로드)는 사용자 승인 없이 진행하지 않는다.
- 오류나 반복된 실수는 `ERROR_LOG.md`에 기록한다.
- 화면 작업 후에는 브라우저에서 직접 확인하고 가능하면 `E2E`(이투이: 사용자 흐름 전체 검증)를 진행한다.
- 현재 이미지 등록/삭제 기록은 브라우저 `localStorage`(로컬스토리지: 브라우저 내부 저장소)에 저장된다.
- 여러 PC와 직원이 같은 영업자료를 공유하려면 추후 백엔드와 `DB`(디비: 데이터를 저장하고 관리하는 데이터베이스)가 필요하다.

## 배포 관련 메모

- 메인 파파인사이트 서버 IP(아이피: 서버 인터넷 주소): `158.247.241.188`
- 서버 배포 경로: `/var/www/papainsight`
- Nginx(엔진엑스: 웹사이트를 보여주는 서버 프로그램) 설정 파일: `/etc/nginx/sites-enabled/papainsight.co.kr`

## 2026-05-28 v0.1 확정 사항

- 현재 최신 화면 버전은 `v0.1`로 표기한다.
- 로그인 페이지에는 테스트 계정 안내를 노출하지 않고, 버전 정보만 작게 표시한다.
- 메인 대시보드에서는 기존 `MVP Console` 문구 위치에 `v0.1`을 표시한다.
- 모든 주요 대시보드 상단에는 현재 버전을 확인할 수 있는 버전 배지를 둔다.
- 영업자료 이미지 카드는 기본적으로 촘촘하게 보이게 유지하고, 사용자가 펼쳐보기를 누르면 느린 확장 모션으로 이미지를 크게 확인할 수 있게 한다.
- 거래처 관리 담당자 하위 메뉴는 담당자 클릭 시 접히지 않고 열린 상태를 유지한다.
- 오른쪽 브라우저 확인용 상단 툴바는 `파파인사이트 프로젝트` 제목, 섹션 전환(`내부 ERP`, `랜딩페이지`), 화면 크기 전환(`데스크톱`, `태블릿`, `모바일`, 직접 px 입력)을 포함한다.
- 랜딩페이지는 `lcm67088-tech/B2CRanding` GitHub 최신본을 `landing-preview/` 폴더로 내려받아 iframe(아이프레임: 다른 웹페이지를 현재 화면 안에 넣는 HTML 요소)으로 표시한다.
- 현재 랜딩페이지 미리보기 연결 파일은 `landing-preview/index-v2.html`이며, 이 파일은 `styles-v2.css`, `script-v2.js`를 사용한다.
- `landing.papainsight.co.kr` 운영 서버에는 `B2CRanding` 최신 v2 작업물을 배포했다. 서버에서는 `index-v2.html`을 운영용 `index.html`로 배치하고, `styles-v2.css`, `script-v2.js`, `assets/`를 함께 배포한다.

## 2026-05-29 GitHub 인수인계 업로드 메모

- 사용자가 다른 PC에서 이어 받을 목적의 업로드를 요청해 현재 작업물을 `sssddd3564-hash/papainsight` 저장소에 반영한다.
- 저장소 업로드 시 실제 운영/개발에 필요한 파일만 포함하고, `_backup_before_github_*`, `_github_latest_*`, `_landing_latest_*`, `github-papainsight`, 스크린샷, 브라우저 덤프, 압축 파일은 제외한다.

## 2026-06-01 GitHub 인수인계 메모

- 사용자가 다른 PC에서 바로 이어 작업할 예정이므로 현재 로컬 작업물을 `sssddd3564-hash/papainsight` 저장소에 커밋하고 푸시한다.
- 현재 루트 작업 폴더에는 `.git`(깃 저장소 메타 폴더)이 없으므로 `_push_workspace_20260529-135950` 클론을 최신화한 뒤 선별 파일을 복사해 커밋한다.
## 2026-06-01 영업자료 이미지 공유 저장 결정

- 영업자료 이미지 등록/삭제 데이터는 더 이상 각 PC의 `localStorage`(로컬스토리지: 해당 브라우저 안 저장소)에만 의존하지 않고, 로컬 서버의 `/api/materials` `API`(에이피아이: 프로그램끼리 데이터를 주고받는 연결 방식)를 우선 사용한다.
- `/api/materials`는 `data/materials-state.json`에 등록 이미지, 삭제된 기본 이미지 ID, 삭제 로그를 함께 저장한다.
- 서버 `API`가 없는 정적 배포 환경에서는 기존처럼 `localStorage`로만 동작하므로, 여러 PC가 같은 자료를 공유하려면 운영 도메인에서도 해당 `API` 또는 별도 `DB`(디비: 데이터를 저장하고 관리하는 데이터베이스) 연결이 필요하다.
- 자료 추가 팝업은 1장, 2장, 3장 이상 다중 이미지 등록을 지원하며, 한 번 등록한 뒤 다시 등록해도 버튼 상태가 복구되도록 오류 처리와 등록 중 버튼 비활성화를 적용한다.

## 2026-06-01 영업자료 보관함 분리

- 영업자료 이미지 화면 상단의 기존 `영업자료 이미지 보관함`을 `단가 및 설명 이미지`와 `레퍼런스 이미지 및 링크` 두 보관함 카드로 분리했다.
- `단가 및 설명 이미지`는 기존 단가표/설명 이미지 카테고리를 관리하는 보관함이며, 기존 기본 이미지와 등록 이미지의 기본 분류는 이 보관함으로 유지한다.
- `레퍼런스 이미지 및 링크`는 상품별 참고 이미지 또는 외부 링크를 등록하는 보관함이다. 링크만 등록하는 자료도 카드로 표시한다.
- 전체 배경과 주요 카드 스타일은 블랙/그레이 기반에 초록/민트 네온 포인트가 들어가는 방향으로 조정했다.
- 두 보관함 상단 카드는 탭처럼 동작한다. `단가 및 설명 이미지`를 클릭하면 해당 보관함 리스트만 보이고, `레퍼런스 이미지 및 링크`를 클릭하면 레퍼런스 리스트만 보인다.
- 공통 카테고리에 `클립 상위노출`을 추가해 양쪽 보관함에서 동일하게 등록할 수 있게 했다.
- 영업자료 카드는 수정 기능을 제공한다. 이름, 카테고리, 링크, 이미지 파일을 수정할 수 있으며, 수정 시 새 이미지를 선택하지 않으면 기존 이미지가 유지된다.
- 영업자료 카드에는 체크박스가 있으며 현재 보관함 기준 전체 선택, 선택 다운로드, 선택 복사를 지원한다.
- 이미지 목록은 한눈에 더 많이 보이도록 촘촘한 카드 그리드로 조정하고, 이미지 클릭 자체로 카테고리 목록이 접히지 않도록 이벤트 흐름을 분리했다.
- 카테고리별 자료 수는 우측 네온 배지 카드로 표시하고, 카테고리별 전체 선택 버튼을 제공한다.
- 보관함+카테고리 단위의 펼침 상태를 기억해 체크/이미지 선택/전체 선택 후에도 사용자가 보고 있던 카테고리가 접히지 않도록 유지한다.
- 삭제 기록은 삭제자 계정명 기준 그룹으로 표시한다. 삭제 기록 데이터는 `/api/materials` 공유 상태의 `deleteLogs`에 저장되므로 다른 PC에서도 같은 기준으로 확인된다.
- 레퍼런스 보관함 링크 등록은 여러 줄 입력을 지원한다. 한 줄에 하나씩 URL(유알엘: 웹 링크 주소)을 입력하면 링크 자료 카드가 여러 개 생성된다.
- 영업자료 카테고리 검색은 자동완성 후보와 함께 제공하며, 입력 키워드가 포함된 카테고리만 보여준다.
- 영업자료 외 인바운드 DB, 영업자료 텍스트, 거래처 관리 화면도 블랙/그레이 기반 네온 카드 톤으로 맞춘다.
## 2026-06-01 영업자료 3분류 보관함 결정

- 기존 `레퍼런스 이미지 및 링크` 보관함은 `레퍼런스 이미지`와 `레퍼런스 링크`로 분리한다.
- 최종 보관함 구조는 `단가 및 설명 이미지`, `레퍼런스 이미지`, `레퍼런스 링크` 3개다.
- `레퍼런스 링크`는 여러 URL을 한 번에 입력할 수 있으며, URL마다 카드를 나누지 않고 하나의 링크 묶음 카드로 저장한다.
- 기존 `reference` 자료는 이미지가 있으면 `reference-image`, 이미지 없이 링크만 있으면 `reference-link`로 자동 분류한다.
- `레퍼런스 링크` 카드 복사 기능은 카드 안의 전체 링크를 줄바꿈 목록으로 복사한다.

## 2026-06-05 계정 추가

- 직원 로그인 계정에 `도영 / 8462` 계정을 추가했다.

## 2026-06-05 영업자료 카테고리 스크롤 보존

- 영업자료(이미지) 카테고리 접기/펼치기, 자료 선택, 전체 선택처럼 목록을 다시 그리는 동작 후에도 사용자가 보던 스크롤 위치를 유지한다.
- 전체 카테고리 검증 기준은 11개 카테고리 각각에 `pricing`, `reference-image`, `reference-link` 테스트 자료를 넣고 공유 저장 API에서 정상 왕복되는지 확인하는 방식으로 진행한다.

## 2026-06-05 영업자료(텍스트) 재구성

- 기존 파파AI 원문 문서 뷰어형 화면을 영업 실무자가 바로 읽을 수 있는 표 대시보드로 교체한다.
- 1차 분류는 `리워드(트래픽)`, `블로그`, `영수증`이다.
- 표 컬럼은 `대분류`, `중분류`, `소분류`, `실행사`, `상품`, `단가`, `구동/수량`, `환불/AS`, `영업 메모`로 고정한다.
- 현재 영수증 자료는 보유 문서 내 언급 중심으로 정리했으며, 추후 전용 영수증 스펙 문서가 들어오면 같은 표 구조에 추가한다.

## 2026-06-05 영업자료 이미지 3단계 카테고리 구조

- 영업자료(이미지) 페이지는 `categories`(카테고리즈: 카테고리 데이터 배열)를 기준으로 `대분류 > 중분류 > 소분류` 3단계 구조를 사용한다.
- 현재 대분류는 `단가 및 설명 이미지` 하나이며, 중분류는 `플레이스`, `오픈마켓`, `그 외 플랫폼`이다.
- 소분류는 `리워드 (트래픽)`, `블로그`, `영수증`, `클립`, `샤오홍슈`, `쇼핑`, `쿠팡`, `SNS 리워드`, `당근마켓`, `홈페이지 제작`이다.
- 중분류 dot 색상은 플레이스 `#185FA5`, 오픈마켓 `#3B6D11`, 그 외 플랫폼 `#BA7517`로 고정한다.
- 검색은 소분류 이름 기준으로 동작하며, 각 소분류의 `등록` 버튼은 기존 이미지 업로드 모달을 해당 소분류로 미리 선택해 연다.

## 2026-06-05 영업자료 이미지 선택형 3단계 구조

- 영업자료(이미지) 화면은 상단 헤더, 대분류 3열 카드, 중분류 3열 카드, 소분류 행 목록 구조로 정리했다.
- 대분류는 `단가 및 설명 이미지`, `레퍼런스 이미지`, `레퍼런스 링크` 3개다.
- `단가 및 설명 이미지`는 `플레이스`, `오픈마켓`, `그 외 플랫폼` 중분류를 가진다.
- `레퍼런스 이미지`, `레퍼런스 링크`는 `hasMid: false`라서 중분류 행과 소분류 목록을 표시하지 않는다.
- 소분류 검색은 현재 선택된 중분류의 소분류명 기준으로 적용된다.
- 소분류 목록의 `자료 추가`와 각 행의 `등록` 버튼은 기존 이미지 업로드 `modal`(모달: 화면 위에 뜨는 입력창)을 해당 소분류로 미리 선택해 연다.

## 2026-06-05 영업자료(텍스트) 화면 정리

- 영업자료(텍스트) 화면은 표 내용 자체가 중심이 되도록 보조 영문 라벨과 검색 박스를 제거한다.
- 상단 페이지의 `ERP DASHBOARD` 보조 라벨도 제거해 화면 밀도를 높인다.

## 2026-06-05 소분류별 이미지 카드 표시 보강

- 소분류 행 아래에 해당 소분류 `categoryId`(카테고리 아이디: 자료가 속한 분류 식별값)를 가진 이미지 카드가 표시되도록 연결했다.
- `등록` 버튼으로 업로드한 이미지는 기존 저장 흐름을 통해 해당 소분류에 저장되고, 렌더링 후 같은 소분류 아래 카드로 보인다.
- 소분류 내부 이미지는 기존 `renderMaterialCard`(렌더 머티리얼 카드: 자료 카드 HTML을 만드는 함수)를 재사용한다.

## 2026-06-05 영업자료 이미지 소분류 카드형 기능 보강

- 영업자료(이미지) 화면의 대분류 3개(`단가 및 설명 이미지`, `레퍼런스 이미지`, `레퍼런스 링크`) 모두에 `플레이스`, `오픈마켓`, `그 외 플랫폼` 중분류와 소분류 구조를 적용했습니다.
- 소분류는 한 줄 긴 목록이 아니라 6열 카드형 그리드로 표시되도록 조정했습니다.
- 소분류 카드를 클릭하면 해당 소분류 자료만 아래에 펼쳐지고, 다른 소분류 자료 영역은 자동으로 닫히는 선택형 구조로 변경했습니다.
- 기존 이미지 자료 카드 기능인 펼쳐보기, 다운로드, 복사, 수정, 삭제를 소분류 내부 카드에서도 그대로 사용할 수 있게 연결했습니다.
- 이미지 카드에서 상단 `이미지 자료` 표시와 파일명(`제목+jpg`) 노출 줄을 제거했습니다.

## 2026-06-05 영업자료 이미지 소분류 헤더 제거

- 영업자료(이미지) 화면에서 소분류 카드 위에 표시되던 `소분류 목록 / 중분류명` 헤더 영역을 제거했습니다.
- `자료 추가` 버튼은 유지하되 별도 액션 영역으로 옮겨 소분류 카드 목록이 더 바로 보이도록 조정했습니다.

## 2026-06-05 영업자료 이미지 대분류 카드 시각 구분 강화

- 영업자료(이미지) 대분류 카드 3개에 서로 다른 키컬러를 적용했습니다.
  - `단가 및 설명 이미지`: 그린 계열
  - `레퍼런스 이미지`: 블루 계열
  - `레퍼런스 링크`: 앰버 계열
- 대분류 카드 제목 텍스트를 더 밝고 굵게 조정하고, 카드별 은은한 네온 라인과 그림자를 추가했습니다.
- `단가 및 설명 이미지`와 `레퍼런스 이미지`가 같은 아이콘으로 보이던 문제를 수정했습니다.

## 2026-06-05 선택 소분류 전체 선택 기능 추가

- 영업자료(이미지) 선택 소분류 영역에 `전체 선택`, `선택 해제`, 선택 개수 표시를 추가했습니다.
- 전체 선택은 현재 펼쳐진 소분류의 자료에만 적용되도록 제한했습니다.
- 기존 자료 카드 체크박스를 그대로 사용해 개별 지정 선택도 함께 유지했습니다.

## 2026-06-05 영업자료 검색 및 텍스트 페이지 카드 구조 정리

- 영업자료(이미지) 소분류 검색에서 `datalist` 추천 목록을 제거하고 실시간 검색만 남겼습니다.
- 이미지 소분류 검색어가 현재 중분류 밖에 있는 경우에도 현재 대분류 안에서 맞는 중분류와 소분류로 자동 이동하도록 수정했습니다.
- 영업자료(텍스트) 화면을 영업자료(이미지)와 같은 대분류 카드, 중분류 카드, 소분류 카드, 선택 소분류 본문 구조로 재구성했습니다.
- 텍스트 화면도 소분류 검색 입력을 제공하고, 검색어가 맞는 중분류와 소분류로 자동 이동하도록 연결했습니다.
- 기존 텍스트 영업자료 표 데이터는 선택된 소분류 본문 안에서 보이도록 유지했습니다.

## 2026-06-05 영업자료(텍스트) 분류 기준 정정

- 영업자료(텍스트) 화면에서 임의로 만든 `단가 및 설명 텍스트`, `레퍼런스 텍스트`, `레퍼런스 링크` 대분류를 제거했습니다.
- 기존 영업자료(텍스트) 데이터 묶음인 `리워드(트래픽)`, `블로그`, `영수증`을 대분류로 사용하도록 정정했습니다.
- 각 대분류 안의 기존 표 데이터 `중분류`, `소분류` 값을 기준으로 중분류 카드와 소분류 카드를 자동 생성하도록 변경했습니다.
- 화면 미장과 카드 구조만 영업자료(이미지)와 비슷하게 유지하고, 내용 기준은 기존 영업자료(텍스트)를 따르도록 정리했습니다.

## 2026-06-05 UI 인터랙션 기준

- 파파인사이트의 포인트 효과는 과하지 않은 그린/시안 네온을 기준으로 적용합니다.
- 버튼, 탭, 대시보드 메뉴, 카테고리 카드는 클릭 시 짧은 네온 스윕으로 반응을 보여줍니다.
- 화면 이동, 표 전환, 접힘/펼침 영역은 즉시 끊기지 않고 슬라이드로 부드럽게 이어지도록 유지합니다.
- 자료 이미지와 텍스트를 읽는 화면이므로 애니메이션은 시선을 빼앗지 않는 0.6초 내외의 은은한 움직임으로 제한합니다.

## 2026-06-05 운영 도메인 상단 작업 툴바 숨김 및 대분류 수량 정리

- 실제 도메인 서버에서는 상단 `파파인사이트 프로젝트` 미리보기/전환 툴바가 보이지 않도록 운영 도메인 감지 클래스를 추가했습니다.
- 로컬 작업 환경(`localhost`, `127.0.0.1`)에서는 기존 작업용 툴바를 계속 사용할 수 있게 유지했습니다.
- 영업자료 대분류 카드에서 `중분류 개수`, `소분류 개수` 배지를 제거했습니다.
- 자료 수량은 대분류 카드 제목 옆의 작은 배지로 표시되도록 정리했습니다.

## 2026-06-05 중분류 카드 수량 표시 정리

- 영업자료 중분류 카드에서 `소분류 개수` 표시를 제거했습니다.
- 중분류 카드도 자료/항목 수량만 제목 옆 작은 배지로 표시되도록 정리했습니다.

## 2026-06-07 로그인 보완 메모

- 기본 관리자 계정은 `papa.admin / 1234`이다.
- 사용자가 로그인 실패를 보고해 관리자 아이디 별칭으로 `admin`, `관리자`도 허용한다.
- 비밀번호는 그대로 `1234`를 사용한다.

## 2026-06-08 GitHub 인수인계 메모

- 사용자가 다른 PC에서 바로 이어 작업할 예정이므로 현재 로컬 변경사항을 `sssddd3564-hash/papainsight` 저장소에 커밋하고 푸시한다.
- 포함 대상은 로그인 보완, 관리자 아이디 별칭, 캐시 방지 버전 갱신, 문서 및 오류 기록이다.
